
// This component has been merged into AdminLayout.tsx for better state management.
// Keeping this file as a shell in case it's imported elsewhere, but it discourages separate usage.
import { Navigate } from 'react-router-dom';

export const AdminSidebar = ({ ...props }: any) => {
    // This shouldn't be used directly anymore
    return null;
};
